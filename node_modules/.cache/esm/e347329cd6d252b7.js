let express,mongoose,cors,readdirSync;_d6d‍.w("express",[["default",["express"],function(v){express=v}]]);_d6d‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_d6d‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_d6d‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);




const morgan = require("morgan");
require("dotenv").config();

const app = express();



// db
mongoose
  .connect(process.env.DATABASE, {
    useNewUrlParser: true,
    useFindAndModify: false,
    useUnifiedTopology: true,
    useCreateIndex: true,
  })
  .then(() => console.log("DB connected"))
  .catch((err) => _d6d‍.g.console.log("DB CONNECTION ERROR => ", err));

// middlewares
app.use(express.json({ limit: "5mb" }));
app.use(express.urlencoded({ extended: true }));
app.use(
  cors({
    origin: [process.env.CLIENT_URL],
  })
);

// autoload routes
//readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));


app.get('/api/register', (req, res) => {
  console.log("Register")
  res.send('Hello from the MERN Stack backend!');
});
const port = process.env.PORT || 8000;

app.listen(port, () => console.log(`Server running on port ${port}`));